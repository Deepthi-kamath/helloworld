#include<stdio.h>
#include<conio.h>
float areaoftriangle(int b,int h)
{
float area;
area=(1/2)*b*h;
return area;
main()
{
int p,q;
float r;
printf("enter base and height");
scanf("%d%d",p,q);
r=area(p,q);
printf("area of triangle is given by %f",r);
getch();
}

